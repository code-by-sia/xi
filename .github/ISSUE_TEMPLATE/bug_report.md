---
name: Bug report
about: Report something that doesn't work as expected
title: "[bug] "
labels: bug
assignees: ''
---

## Description

A clear description of the bug.

## Minimal reproduction

The smallest `.x` program that triggers it:

```x
// paste here
```

Command you ran (e.g. `./compiler/xc foo.x`, `./bin/x`, `./compiler/selfhost.sh`):

```sh

```

## Expected vs. actual

- **Expected:**
- **Actual:** (paste the full output / error, e.g. `xc: file.x:LINE: error: ...`)

## Environment

- X version / release tag or commit:
- OS and arch (e.g. `macos-arm64`, `linux-x86_64`):
- C compiler (`cc --version` first line):
- Built via `bootstrap.sh`, a downloaded release, or `XC_SEED`?

## Additional context

Anything else relevant (related issues, recent changes, etc.).
