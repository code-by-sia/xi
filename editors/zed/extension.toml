id = "xi"
name = "Xi"
version = "0.1.1"
schema_version = 1
authors = ["The Xi programming language"]
description = "Syntax highlighting and language support for the Xi programming language."
repository = "https://github.com/code-by-sia/xi"

# The grammar lives in this same repo under editors/tree-sitter-xi (Zed's `path`
# points at the subdirectory). `commit` is a commit SHA of THIS repo — update it
# whenever the grammar changes (and re-install the extension).
[grammars.xi]
repository = "https://github.com/code-by-sia/xi"
commit = "3596114afaf1452bf90582100b2d42631f7f4e5e"  # <- set to `git rev-parse HEAD`
path = "editors/tree-sitter-xi"
