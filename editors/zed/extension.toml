id = "x"
name = "X"
version = "0.0.1"
schema_version = 1
authors = ["The X programming language"]
description = "Syntax highlighting and language support for the X programming language."
repository = "https://github.com/code-by-sia/x"

# The grammar lives in this same repo under editors/tree-sitter-x (Zed's `path`
# points at the subdirectory). `commit` is a commit SHA of THIS repo — update it
# whenever the grammar changes (and re-install the extension).
[grammars.x]
repository = "https://github.com/code-by-sia/x"
commit = "d869c646bd25c832d8aecaad4464880eb492cb38"  # <- set to `git rev-parse HEAD`
path = "editors/tree-sitter-x"
